# Credit Risk Gap Register (spec `0072`)

Observed gaps between the credit capability surface and what this repository
actually has. Identification is **not** correction: a row leaves this register
only when an artifact and its validation evidence change, and the disposition
says which spec owns that.

Severity: `high` blocks any claim of coverage for the capability and blocks
`reviewed` promotion of records that depend on it. `medium` limits the claim.
`low` is a known rough edge.

| ID | Gap | Evidence | Severity | Affected | Disposition | Owner |
| --- | --- | --- | --- | --- | --- | --- |
| G-0072-001 | No named credit-domain reviewer existed, so no record in this pack could be promoted beyond `draft`. | Was: `knowledge/credit_risk/README.md` review policy; every record carried `review_status: draft` and `review: null`. | high | Every capability; the whole pack. | **Resolved 2026-09-11.** Reviewer named: Joshua Lutkemuller, CFA (repository owner and spec approver). Stated plainly, not glossed over: this is the account owner's own attestation, not an independent third-party credit officer or model validator's sign-off — the review scope for each promoted record is stated individually in its `review.scope` field, and remains a self-attested review for a portfolio SDK, not SR 11-7-grade independent validation. Naming the reviewer unblocks promotion; it does not itself promote every record — records still affected by an open high-severity gap (`G-0072-002`, `G-0072-005`) stay `draft` via their own `blocked_by_gap_ids` field, enforced by the validator, not by memory. | `0072` T-016 |
| G-0072-002 | No credit measurement runtime existed. PD, LGD, EAD, and rating conventions were contracts with golden cases, not code. | Was: `coverage.json` capability rows at `contract_only`; no module under `src/quantsmith/pipelines/` computed a credit measure. | high | `capability.wholesale_measurement`, `capability.counterparty_limits` | **Resolved by `0073`.** `wholesale_credit_measurement.py` implements facility-level EAD/EL/RWA measurement (composing `credit_risk_knowledge.py`'s own arithmetic, not reimplementing it) and the full counterparty exposure/limit/concentration workflow. Rating assignment and PD/LGD estimation remain out of scope by design — `workflow.wholesale_obligor_review` stays `adopter_plugin_via_0026`; only the measurement step this capability actually claims is now runtime, not the regulated model an institution must own. The eight records this gap blocked are promoted to `reviewed` with content unchanged; only the blocking condition closed. | `0073` |
| G-0072-003 | The lifetime PD term-structure method is deliberately unset, so lifetime ECL cannot be computed end to end from a one-year PD. | `conventions.json` `param.pd.term_structure_method` has `default: null, required: true`. | medium | `capability.expected_credit_loss` | Accepted by design, not deferred by omission. There is no universal correct method; a default here would silently impose ours on every adopter. The parameter must be supplied. | `0075` |
| G-0072-004 | The IRB risk weight is an input, so no capital number can be produced from this pack alone. | `conventions.json` `param.rwa.risk_weight` has `default: null, required: true`; `golden.rwa.irb_risk_weight` supplies it as an input with declared provenance. | medium | `capability.regulatory_capital` | Accepted by design. National implementation differs from the international framework; a hard-coded weight would make a wrong capital number look authoritative. | `0076` |
| G-0072-005 | No fairness harness exists. The contract now requires substantive fairness testing (REQ-020), and the disparity screen is computable, but `hook.disparate_impact.*` is still a declared identifier with no implementation, and per-feature proxy association is required-and-declared rather than measured. | `decision_paths.json` names the hooks; `adverse_impact_ratio` computes the screen; no module measures feature-level association or runs an LDA search. | high | `capability.retail_underwriting` | Assigned. `0074` must ship the harness before any retail scoring path moves past `contract_only`. The contract can prove a workflow *declares* and *is obliged to* test, not that the testing was performed on real outcomes. | `0074` |
| G-0072-011 | REQ-014 originally required only that protected attributes be absent from the feature set, which certifies nothing on its own — disparate impact arises from correlated neutral features. Found by audit of the built pack, not before implementation. | Pre-amendment `decision_paths.json` had no proxy, disparity, or less-discriminatory-alternative concept; `grep` for "proxy" across the spec and pack returned nothing. | high | `capability.retail_underwriting` | **Corrected in `0072`.** REQ-020, `convention.fairness.disparity_testing`, fairness obligations on every consumer path, `golden.fairness.adverse_impact_ratio`, and AC-023/AC-024 with per-obligation negative tests. REQ-014 now states in its own text that attribute absence is not sufficient. Retained here rather than deleted: the register records what was wrong and when, not only what is right now. | `0072` |
| G-0072-006 | No governed credit corpus existed, so the LLM evidence boundary was unexercised on anything but hand-typed fixtures written to pass it. | `0071`'s only registered corpus was `text_intelligence_fixture`, generic and not credit-specific. | medium | `capability.document_intelligence` | **Partially corrected in `0077`.** A dedicated `credit_document_fixture` source is registered; the admission boundary is proven against a real, emitted `0071` bundle (both outcomes plus rejection), not hand-typed dicts. Still open: a real, licensed corpus at retrieval scale, and a genuine text-dependent extractor -- `0077` deliberately proves wiring, not extraction accuracy; both remain future work with no owning spec yet. | `0077` (wiring, done); unnumbered (real corpus + extractor, not yet reserved) |
| G-0072-007 | Credit sources are registered as locators only; nothing is ingested, and no vintage control is exercised against a real publication. | `sources/{cfpb,fasb,ifrs_foundation,bis_basel,occ}.yml` all carry `status: evaluating` and `method: manual_download`. | medium | Every capability that cites a source. | Assigned. Live ingestion, vintage capture, and data contracts belong to `0078`, matching how `0063` deferred its own ingestion to `0068`. | `0078` |
| G-0072-008 | Counterparty credit concentration has no runtime, and the nearest existing one measures something else. | `0038`'s `factor_risk_model.py` decomposes market factor risk and concentration, not counterparty credit exposure. | medium | `capability.counterparty_limits` | Assigned. The distinction is recorded here precisely so `0038` is not mistaken for coverage because the word "concentration" appears in both. | `0073` |
| G-0072-009 | No monitoring runtime exists, so the deployability predicate can check that a monitoring plan is *declared*, not that monitoring runs. | `governance.json` `gov.monitoring_plan` requires the artifact; nothing measures drift or calibration. | medium | `capability.model_governance` | Assigned. Whether the runtime should be credit-specific or generalized across the SDK's whole model inventory is an open question in `0072`'s spec. | `0079` |
| G-0072-010 | The pack asserts that IFRS 9 and CECL differ but does not yet enumerate where, beyond staging and lifetime scope. | `conventions.json` holds one lifetime-ECL convention citing both `ifrs_foundation` and `fasb`. | low | `capability.expected_credit_loss` | Open. Splitting the convention is likely correct but should follow a real consumer rather than precede one, so the split is driven by a difference that matters to someone. | `0075` |

## What this register is not

It is not a list of everything credit risk contains. It is a list of gaps
between the **declared capability surface** in `coverage.json` and what this
repository has. Products, jurisdictions, and portfolios outside that surface —
non-U.S. jurisdictions, insurance and sovereign credit, securitization and
structured credit, counterparty valuation adjustments, credit derivative
pricing — are recorded as out of scope in `0072`'s spec, not as gaps here.
