# Short-Term Markets Gap Register

This register is the required 0063 disposition log for current short-term
markets weaknesses. A row is not evidence of a runtime fix; it is evidence that
the issue is visible, scoped, and owned.

| ID | Gap | Evidence | Severity | Disposition | Owning spec |
| --- | --- | --- | --- | --- | --- |
| D-0063-001 | Repo-special versus stock-loan-special sign and viewpoint ambiguity. | `instructions/securities_financing.md` currently says to distinguish GC from specials in both stock loan and repo without a role/sign conversion; `agents/securities_financing/repo_financing/instructions.md` asks for GC vs specials but does not define the repo-rate axis; `agents/securities_financing/securities_lending/instructions.md` treats specials through the higher borrow-cost lens. | high | Corrected at the 0063 contract layer by `concept.repo_special`, `concept.stock_loan_special`, `convention.repo.specialness_bps`, and `golden.repo.specialness_sign`. `agents/securities_financing/securities_lending/instructions.md` now cites both concept IDs by name (spec `0066`). Repo-runtime compatibility remains `0064`'s job. | 0064 for repo runtime compatibility; 0066 for securities-lending compatibility (doc slice done) |
| D-0063-002 | Hard-coded securities-lending GC/WARM/HTB classification cutoffs can look like universal market rules. | `src/quantsmith/quant/agentic_quant/sec_lending.py:103` and `:104` define `_GC_THRESHOLD = 50` and `_WARM_THRESHOLD = 200`; `_classify()` applies them globally. | high | **Corrected (spec `0066`).** Thresholds are now `SecLendingUniverseAgent.__init__(gc_threshold_bps=50.0, warm_threshold_bps=200.0)` constructor params, defaulting to the prior values (`convention.seclend.demo_classification_thresholds`, still a draft model assumption, not sourced market truth) and overridable per caller. `test_classification_thresholds_are_configurable_D_0063_002`. | 0066 (done) |
| D-0063-003 | Mixed accrual bases: existing securities-lending runtime uses ACT/252-style daily fee approximations while financing-cost analysis uses ACT/360. | `src/quantsmith/quant/agentic_quant/sec_lending.py:258`, `:427`, and `:589` divide fee calculations/annualization by 252; `src/quantsmith/pipelines/financing_cost_analysis.py:40` sets `_DAY_COUNT_BASIS = 360.0` and `:118` uses it for all legs. | high | **Corrected (spec `0066`).** `sec_lending.py` now uses a named `_DAY_COUNT_BASIS = 360.0` (ACT/360) at every fee/annualization site, matching `financing_cost_analysis.py` and `convention.seclend.{borrow_fee,rebate}_act360_simple`. `test_daily_fee_uses_act360_matches_seclend_golden_case_D_0063_003` reproduces `golden.seclend.fee_rebate_signs`'s exact 30-day totals. Product-specific extensions remain `0064`/`0065`'s job. | 0066 (done); 0064/0065 for product-specific extensions |
| D-0063-004 | Securities-lending allocation declares counterparty concentration but does not enforce it inside the optimizer. | `src/quantsmith/quant/agentic_quant/sec_lending.py:380` through `:390` document a counterparty constraint, but `_optimize()` builds only a balance-sheet `A_ub` at `:435` through `:437`; risk concentration is checked later at `:528` through `:565`. | medium | **Corrected (spec `0066`), "honest fix."** The false claim is removed rather than answered with fabricated per-counterparty demand data (the SQL schema has no per-security×counterparty demand to allocate against). `InventoryOptimizationAgent`'s docstring now states plainly this LP has no counterparty dimension; `OptimizedAllocation.counterparty` is always `None`, not the prior `"BEST_AVAILABLE"` placeholder. `SecLendingRiskAgent` (unchanged, already correct) remains the actual, tested counterparty-concentration enforcement point. `test_optimizer_allocations_have_no_counterparty_assignment_D_0063_004`. A real per-counterparty LP constraint remains possible only if a future spec adds real demand data to the SQL model. | 0066 (done, honest-fix scope) |
| D-0063-005 | Financing-cost point-in-time check can miss rates learned during a holding period and applied retroactively to earlier days. | `src/quantsmith/pipelines/financing_cost_analysis.py:235` through `:245` flags only a `rate_asof` after `period_end`; a rate known after `period_start` but before `period_end` can still be applied to the whole period by the caller. | high | **Corrected (spec `0066`).** `check_point_in_time` now flags `rate_asof > period_start` (not `period_end`), matching `golden.temporal.asof_excludes_later_known_rate`'s "knowledge_as_of must be on or before the query as_of date" rule. Segmenting financing legs into sub-periods so a mid-period rate could be admitted for its own later sub-period remains deliberately out of scope. `test_check_point_in_time_flags_midperiod_lookahead_D_0063_005`. | 0066 (done) |
| D-0063-006 | Financing-cost analysis uses fixed notional and one universal day-count basis for every leg. | `src/quantsmith/pipelines/financing_cost_analysis.py:78` through `:84` define one notional and period per position; `:40` and `:118` apply ACT/360 to every leg. | medium | **Partially corrected (spec `0066`).** `FinancingLeg.day_count_basis` is now an explicit, overridable field (default 360.0, preserving prior behavior); a leg can declare a different convention when its product needs one. `test_leg_day_count_basis_is_explicit_and_overridable_D_0063_006`. Variable notional and full cashflow scheduling remain out of scope, deferred to `0064`/`0065`. | 0066 (day-count slice done); 0064, 0065 for variable notional/scheduling |

## Review Notes

- All six mandatory discrepancies from REQ-012 are retained here.
- Five of six (D-0063-002 through D-0063-006) are now runtime-corrected by
  spec `0066`, each with a passing test tied to a `0063` `golden.*` record.
  D-0063-001's remaining securities-lending doc slice is also done; its
  repo-runtime slice stays `0064`'s job.
- Records affected by unresolved severity-high gaps must remain `draft` until
  the owning spec supplies implementation and validation evidence, or a named
  reviewer explicitly scopes the record around the gap.
