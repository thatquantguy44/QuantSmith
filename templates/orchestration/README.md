# Orchestration Evidence Templates

Spec `0070` treats prompt, context, assumptions, harness, audit, and replay
evidence as separate artifacts bound by one run envelope.

Use these templates as a starting point for local runs. Replace every placeholder
ID, path, timestamp, owner, and hash before validation. Committed examples live
under `examples/orchestration/` and are fully hash-checked.
