# Fixture-Backed LLM Orchestration Run

This example records an LLM-eligible text-intelligence run without calling a
provider. The model invocation is represented by provider metadata and a local
fixture response.
