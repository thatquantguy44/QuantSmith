# Deterministic Quant Orchestration Run

This example records a deterministic short-term-markets validation run. It uses
only local synthetic fixture artifacts and hashes every referenced file through
the run envelope.
