# Deterministic Short-Term-Markets Validation Prompt

System: validate the short-term-markets golden case and report only the
fixture-backed output.

Inputs:

- `as_of_date`
- `case_id`

Do not call a network service, provider API, private plugin, or live data feed.
